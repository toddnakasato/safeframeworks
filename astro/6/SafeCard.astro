---
/**
 * SafeCard — Astro 6 card component.
 * Outputs data-* attributes for intent. No hardcoded CSS.
 */
import type { ConfigBase, OnSafeEvent } from 'safecontracts';
interface Props { config: ConfigBase; onEvent?: OnSafeEvent; }
const { config, onEvent } = Astro.props;
---

<div
  data-component="card"
  data-variant={config.metadata.variant}
  data-surface={config.metadata.surface}
  data-spacing={config.metadata.spacing}
  data-radius={config.metadata.radius}
  data-density={config.metadata.density}
>
  {config.metadata.title && <div data-role="header">{config.metadata.title}</div>}
      {config.metadata.subtitle && <div data-role="subtitle">{config.metadata.subtitle}</div>}
      <slot />
</div>
