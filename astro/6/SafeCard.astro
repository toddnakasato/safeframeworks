---
/**
 * SafeCard — Astro 6 record display card.
 * Renders via shared-mapping card builder (./card) — identical across
 * frameworks. Server-renders the shell; client script mounts the
 * component. Structure + data-* only.
 */
import type { ConfigBase, OnSafeEvent } from 'safecontracts';
interface Props { config: ConfigBase; onEvent?: OnSafeEvent; }
const { config } = Astro.props;
---

<div data-card-config={JSON.stringify(config)}></div>

<script>
  import { initSafeCards } from "./card";
  initSafeCards();
</script>
