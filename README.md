# safeframeworks

The blocks. 27 components × 8 framework targets. Component workbench — not a product, not a safebuild.

Build one component, see it across all frameworks and versions. Confirm it renders. Confirm its events fire. Prove it matches the contract. Per component, per framework, per version.

See [SAFEFRAMEWORKS.md](../docs/SAFEFRAMEWORKS.md) for full documentation.
